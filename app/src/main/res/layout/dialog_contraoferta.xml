<?xml version="1.0" encoding="utf-8"?>
<!-- Contenido del dialogo de contraoferta: que te ofrecieron, el precio
     publicado como tope, el nuevo precio y un mensaje breve opcional. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="16dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/tvOfertaActual"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="14sp"
        tools:text="Te ofrecieron $ 160.000 por “Monitor Samsung 24 curvo”" />

    <TextView
        android:id="@+id/tvPrecioPublicado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="12sp"
        android:visibility="gone"
        tools:text="Precio publicado: $ 175.000"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:text="$"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="18sp" />

        <EditText
            android:id="@+id/etMonto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:autofillHints=""
            android:hint="@string/contraoferta_monto_hint"
            android:inputType="numberDecimal"
            android:maxLines="1"
            android:textSize="18sp" />

    </LinearLayout>

    <EditText
        android:id="@+id/etMensaje"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints=""
        android:hint="@string/contraoferta_mensaje_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:maxLength="200"
        android:maxLines="3"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/tvContador"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="12sp"
        tools:text="0/200" />

</LinearLayout>
