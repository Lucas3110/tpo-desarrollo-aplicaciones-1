<?xml version="1.0" encoding="utf-8"?>
<!-- Contenido del dialogo de cambio de email (Punto 2). Un solo layout para los
     dos pasos: primero el email nuevo y despues el codigo que llega a ese
     email. El fragment muestra uno u otro campo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="16dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/tvInstruccion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="14sp"
        tools:text="Ingresá el email nuevo. Te enviamos un código para confirmarlo." />

    <EditText
        android:id="@+id/etEmailNuevo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:autofillHints="emailAddress"
        android:hint="@string/cambiar_email_hint"
        android:inputType="textEmailAddress"
        android:maxLines="1" />

    <EditText
        android:id="@+id/etCodigo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:autofillHints="smsOTPCode"
        android:gravity="center"
        android:hint="@string/otp_codigo_hint"
        android:inputType="number"
        android:letterSpacing="0.3"
        android:maxLength="6"
        android:textSize="24sp"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:visibility="gone" />

</LinearLayout>
